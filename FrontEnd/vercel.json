{
  "version": 2,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/dist/$1"
    }
  ],
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "installCommand": "npm install && chmod +x node_modules/.bin/*",
  "buildCommand": "npx --yes @expo/cli@latest export:web",
  "env": {
    "NODE_ENV": "production",
    "EXPO_PUBLIC_API_BASE_URL": "https://askjc-chatbot.onrender.com"
  },
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cross-Origin-Embedder-Policy",
          "value": "cross-origin"
        }
      ]
    }
  ]
}