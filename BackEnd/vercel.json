{
  "version": 2,
  "buildCommand": "npm install && npx prisma generate && npx tsc",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}